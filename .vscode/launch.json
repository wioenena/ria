{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ria",
            "type": "lldb",
            "program": "${workspaceFolder}/target/debug/ria.exe",
            "args": ["${workspaceFolder}/examples/attributes.ria"],
            "request": "launch",
            "sourceLanguages": ["rust"],
            "preLaunchTask": "rust: cargo build",
            "cwd": "${workspaceFolder}"
        }
    ]
}